apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pilot.fullname" . }}-config
  labels:
    {{- include "pilot.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
