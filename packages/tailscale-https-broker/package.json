{
  "name": "@pilotai/tailscale-https-broker",
  "version": "0.1.0",
  "private": true,
  "license": "LicenseRef-Proprietary",
  "type": "module",
  "description": "Least-privilege host broker that manages only Pilot-owned, same-number Tailscale HTTPS-to-loopback listeners for managed branch runtimes.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "bin": {
    "pilot-tailscale-https-broker": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc && node scripts/build-native.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/build-native.mjs && vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=24.11.0"
  }
}
